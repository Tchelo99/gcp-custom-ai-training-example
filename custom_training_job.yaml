workerPoolSpecs:
  - machineSpec:
      machineType: n1-standard-4
    replicaCount: 1
    containerSpec:
      imageUri: gcr.io/your-gcp-project-id/simple-ai-training:latest
      args:
        - --model-dir=$(AIP_MODEL_DIR)
        - --data-path=gs://your-bucket/path/to/image/data
        - --learning-rate=0.001
        - --batch-size=32
        - --epochs=10